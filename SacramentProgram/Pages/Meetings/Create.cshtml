@page
@model SacramentProgram.Pages.Meetings.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Meeting</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Meeting.MeetingDate" class="control-label"></label>
                <input asp-for="Meeting.MeetingDate" class="form-control" />
                <span asp-validation-for="Meeting.MeetingDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Meeting.WardName" class="control-label"></label>
                <input asp-for="Meeting.WardName" class="form-control" />
                <span asp-validation-for="Meeting.WardName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Meeting.Conducting" class="control-label"></label>
                <select asp-for="Meeting.ConductingID" class="form-control"
                        asp-items="@Model.PersonDropDown">
                    <option value="">-- Select Person --</option>
                </select>
                <span asp-validation-for="Meeting.ConductingID" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="Meeting.Presiding" class="control-label"></label>
                <select asp-for="Meeting.PresidingID" class="form-control"
                        asp-items="@Model.PersonDropDown">
                    <option value="">-- Select Person --</option>
                </select>
                <span asp-validation-for="Meeting.PresidingID" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="Meeting.Accompaniment" class="control-label"></label>
                <select asp-for="Meeting.AccompanimentID" class="form-control"
                        asp-items="@Model.PersonDropDown">
                    <option value="">-- Select Person --</option>
                </select>
                <span asp-validation-for="Meeting.AccompanimentID" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="Meeting.LeadingMusic" class="control-label"></label>
                <select asp-for="Meeting.LeadingMusicID" class="form-control"
                        asp-items="@Model.PersonDropDown">
                    <option value="">-- Select Person --</option>
                </select>
                <span asp-validation-for="Meeting.LeadingMusicID" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="Meeting.OpeningSong" class="control-label"></label>
                <select asp-for="Meeting.OpeningSongID" class="form-control"
                        asp-items="@Model.SongDropDown">
                    <option value="">-- Select Song --</option>
                </select>
                <span asp-validation-for="Meeting.OpeningSongID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Meeting.OpeningPrayer" class="control-label"></label>
                <select asp-for="Meeting.OpeningPrayerID" class="form-control"
                        asp-items="@Model.PersonDropDown">
                    <option value="">-- Select Person --</option>
                </select>
                <span asp-validation-for="Meeting.OpeningPrayerID" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="Meeting.SacramentSong" class="control-label"></label>
                <select asp-for="Meeting.SacramentSongID" class="form-control"
                        asp-items="@Model.SongDropDown">
                    <option value="">-- Select Song --</option>
                </select>
                <span asp-validation-for="Meeting.SacramentSongID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Meeting.MusicalNumber" class="control-label"></label>
                <select asp-for="Meeting.MusicalNumberID" class="form-control"
                        asp-items="@Model.MusicNumDropDown">
                    <option value="">-- Select Musical Number --</option>
                </select>
                <span asp-validation-for="Meeting.MusicalNumberID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Meeting.IntermediateSong" class="control-label"></label>
                <select asp-for="Meeting.IntermediateSongID" class="form-control"
                        asp-items="@Model.SongDropDown">
                    <option value="">-- Select Song --</option>
                </select>
                <span asp-validation-for="Meeting.IntermediateSongID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Meeting.ClosingSong" class="control-label"></label>
                <select asp-for="Meeting.ClosingSongID" class="form-control"
                        asp-items="@Model.SongDropDown">
                    <option value="">-- Select Song --</option>
                </select>
                <span asp-validation-for="Meeting.ClosingSongID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Meeting.ClosingPrayerId" class="control-label"></label>
                <select asp-for="Meeting.ClosingPrayerIdID" class="form-control"
                        asp-items="@Model.PersonDropDown">
                    <option value="">-- Select Person --</option>
                </select>
                <span asp-validation-for="Meeting.ClosingPrayerIdID" class="text-danger" />
            </div>
           
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
